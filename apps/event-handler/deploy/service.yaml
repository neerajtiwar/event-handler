apiVersion: v1
kind: Service
metadata:
  name: event-handler
  namespace: flask-project
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8001
  selector:
    app: event-handler